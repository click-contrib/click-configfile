# =====================================================
# INVOKE CONFIGURATION:
# =====================================================
# -- ON WINDOWS:
# run:  
#   pty: false
#   shell: C:\Windows\System32\cmd.exe
# =====================================================

run:
    echo: true
    pty: true

clean_all:
    extra_directories:
      - .eggs
      - tmp
